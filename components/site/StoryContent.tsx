"use client";
import { useEffect, useState } from "react";
import { defaultStoryContent, PAGE_CONTENT_EVENT, readLocalContent, STORY_CONTENT_KEY } from "@/lib/page-content";
export default function StoryContent(){const [content,setContent]=useState(defaultStoryContent);useEffect(()=>{const sync=()=>setContent(readLocalContent(STORY_CONTENT_KEY,defaultStoryContent));sync();window.addEventListener(PAGE_CONTENT_EVENT,sync);window.addEventListener("storage",sync);return()=>{window.removeEventListener(PAGE_CONTENT_EVENT,sync);window.removeEventListener("storage",sync);};},[]);return <section className="simple-page"><h1>{content.pageTitle}</h1><p>{content.introduction}</p>{content.image&&<img className="simple-page-image" src={content.image} alt={content.pageTitle}/>}<div className="simple-page-section"><h2>{content.philosophyTitle}</h2><p>{content.philosophyBody}</p></div></section>;}
