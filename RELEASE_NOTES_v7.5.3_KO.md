# RELEASE NOTES v7.5.3

## 프로젝트 2열 → 1열 반응형 전환 순서 수정

- 데스크톱 2열에서는 기존 좌우 교차 배치를 유지합니다.
- 모바일 1열에서는 좌우 열 분할 결과를 재사용하지 않고 최신순 원본 배열을 그대로 표시합니다.
- 프로젝트는 `updatedAt`, `createdAt`, `order` 순으로 최신 항목이 먼저 표시됩니다.
- Gallery는 검색 점수가 같을 때 최신 이미지가 먼저 표시됩니다.
- SCRAP은 가장 최근에 저장한 이미지가 항상 맨 앞에 표시됩니다.
- 이미지의 가로/세로 방향은 정렬 기준에 영향을 주지 않습니다.
