# R2 저장 안정성 개선 보고서

이번 수정은 R2 저장용량을 늘리는 기능 추가가 아니라, 이미지 저장 실패와 불필요한 파일 누적을 줄이기 위한 안정성 개선입니다.

## 적용 내용

1. **이미지 바이너리 업로드 경로 추가**
   - 관리자에서 WebP로 최적화한 이미지를 더 이상 큰 Base64 문자열을 프로젝트/갤러리/CMS JSON 안에 넣어 보내지 않습니다.
   - `/api/admin/media-upload`로 이미지 바이너리를 먼저 R2에 저장하고, 이후 DB에는 R2 URL만 저장합니다.
   - 기존 Base64 처리 코드는 이전 데이터 호환을 위해 서버에 그대로 유지했습니다.

2. **저장 순서 안전화**
   - 새 이미지 저장 → D1 저장 성공 → 기존 이미지 삭제 순서로 동작하도록 정리했습니다.
   - 전체 프로젝트 동기화는 새 이미지를 먼저 준비하고 D1 `batch()` 저장이 성공한 뒤에만 삭제된 프로젝트/교체된 옛 이미지를 정리합니다.

3. **실패 시 임시 파일 자동 정리**
   - 이미지 업로드 후 프로젝트/갤러리/CMS 저장이 실패하면 이번 요청에서 새로 업로드된 R2 파일을 자동 삭제합니다.
   - 서버에서 기존 Base64 호환 업로드가 발생한 경우에도 D1 저장 실패 시 새 R2 파일을 되돌립니다.

4. **기존 WebP 최적화 유지**
   - 이미지 최대 크기와 WebP 품질 설정은 기존 값을 유지했습니다.
   - 저장 이미지 수나 최종 R2 파일 크기를 의도적으로 늘리는 변경은 없습니다.

5. **R2 공개 설정 불필요**
   - 기존과 동일하게 `PROJECT_MEDIA` R2 Binding을 사용합니다.
   - Public Access나 R2 API Secret을 프론트엔드에 넣을 필요가 없습니다.

## Cloudflare 연결 시 필요한 값

- R2 Binding variable name: `PROJECT_MEDIA`
- D1 Binding variable name: `DB`

R2 Binding을 추가 또는 변경한 뒤에는 Cloudflare Pages 프로젝트를 재배포해야 합니다.

## 검수 결과

- `functions/**/*.js` 전체 `node --check`: 통과
- 신규 `lib/r2-media-client.ts` 독립 TypeScript strict 검사: 통과
- 전체 프로젝트는 배포 의존성(`next`, `react` 등)이 ZIP에 포함되어 있지 않아 이 작업 환경에서 전체 `next build`를 실행하지 않았습니다. Cloudflare 배포 빌드에서 최종 확인이 필요합니다.
