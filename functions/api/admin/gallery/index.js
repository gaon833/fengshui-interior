import { ensureAdminTables,json,validateSession } from "../../../_shared/admin-auth.js";
import { deleteGallery,ensureGalleryTable,listGallery,upsertGallery } from "../../../_shared/gallery-cms.js";
async function auth(context){if(!context.env.DB)return{response:json({ok:false,error:"D1 바인딩 DB가 없습니다."},503)};await ensureAdminTables(context.env.DB);if(!await validateSession(context.env.DB,context.request))return{response:json({ok:false,error:"Unauthorized"},401)};await ensureGalleryTable(context.env.DB);return{db:context.env.DB}}
export async function onRequestGet(context){try{const a=await auth(context);if(a.response)return a.response;return json({ok:true,items:await listGallery(a.db)});}catch(error){return json({ok:false,items:[],error:error instanceof Error?error.message:"갤러리를 불러오지 못했습니다."},500)}}
export async function onRequestPost(context){try{const a=await auth(context);if(a.response)return a.response;const body=await context.request.json();const items=Array.isArray(body?.items)?body.items:body?.item?[body.item]:[];const stored=[];for(const item of items){if(!item?.id||!item?.src)continue;stored.push(await upsertGallery(a.db,context.env.PROJECT_MEDIA,item));}return json({ok:true,items:stored});}catch(error){return json({ok:false,error:error instanceof Error?error.message:"갤러리 저장에 실패했습니다."},500)}}
export async function onRequestDelete(context){try{const a=await auth(context);if(a.response)return a.response;const body=await context.request.json();if(!body?.id)return json({ok:false,error:"id가 필요합니다."},400);await deleteGallery(a.db,context.env.PROJECT_MEDIA,body.id);return json({ok:true});}catch(error){return json({ok:false,error:error instanceof Error?error.message:"갤러리 삭제에 실패했습니다."},500)}}
