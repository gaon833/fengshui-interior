# Project documentation

The runtime source is kept at the repository root. Historical release notes, patch reports, and deployment investigations are archived under `docs/history/` so they do not obscure the active code.

Active maintenance rules are in `/AGENTS.md`. The current release marker remains `/VERSION.txt`.
