# v7.6.0 유동형 반응형 시스템

- 기존 컴포넌트 구조와 모바일 UI를 유지하면서 새 `styles/fluid-responsive.css`를 최종 레이어로 추가했습니다.
- 1600px/1100px 구간에서 상세 레이아웃을 강제로 덮던 하드코딩을 최종 레이어에서 무효화했습니다.
- 사이드바 폭은 901px 이상에서 320px~400px 범위로 자연스럽게 변화합니다.
- 모든 고정 사이드바 페이지의 콘텐츠 시작점이 동일한 사이드바 변수에 연결됩니다.
- 홈, 일반 콘텐츠 페이지, 프로젝트 목록, 프로젝트 상세, 이전/다음 영역, Gallery, SCRAP의 여백과 간격을 `clamp()` 기반으로 통일했습니다.
- 프로젝트 상세의 기준값은 대형 PC에서 위 200px, 오른쪽 360px, 아래 170px이며 화면 폭에 따라 연속적으로 축소됩니다.
- Gallery/SCRAP의 열 전환은 유지하되 카드 간격은 화면 크기에 따라 연속적으로 줄어듭니다.
- 900px 이하에서는 기존 모바일 1열 구조를 유지하면서 안전 영역과 좌우 여백만 부드럽게 조절합니다.
- `prefers-reduced-motion` 설정은 기존대로 유지합니다.
