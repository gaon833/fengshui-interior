# D1 + R2 최종 운영 설정 및 검수 보고서

## 최종 저장 구조
- D1 바인딩 이름: `DB` (기존 설정 그대로)
- R2 바인딩 이름: `PROJECT_MEDIA`
- 프로젝트 정보: D1 `cms_projects`
- 사이트/메인/SEO/OUR STORY/PROCESS 정보: D1 `cms_content`
- GALLERY 정보: D1 `cms_gallery`
- 관리자 업로드 이미지: R2 `PROJECT_MEDIA`
- 브라우저 localStorage: 서버 장애 시 표시용 캐시/기존 데이터 호환용으로만 유지

## 이미지 최적화
관리자에서 선택한 고화질 원본은 브라우저에서 먼저 최적화한 뒤 서버로 전송합니다.
- 출력 형식: WebP
- 최대 폭: 2400px
- 최대 높이: 3200px
- 품질: 90%
- 원본 비율 유지
- 고품질 Canvas 리샘플링 사용
- 원본 파일 최대 허용: 기존 코드 기준 30MB

따라서 원본 JPEG/PNG가 그대로 R2에 저장되는 것이 아니라, 웹 표시용 고화질 WebP로 줄어든 뒤 R2에 저장됩니다.

## 적용 범위
1. 프로젝트 PC 대표 이미지
2. 프로젝트 모바일 대표 이미지
3. 프로젝트 상세 이미지
4. 프로젝트 OG 이미지
5. GALLERY 업로드 이미지
6. 메인 PC 이미지
7. 메인 모바일 이미지
8. 로고
9. Open Graph 이미지
10. 파비콘
11. OUR STORY 대표 이미지
12. PROCESS 대표 이미지

## 데이터 동기화/호환
- 프로젝트: 기존 localStorage 데이터가 있고 서버 프로젝트가 비어 있으면 자동 서버 이전
- GALLERY: 기존 localStorage 데이터가 있고 서버 갤러리가 비어 있으면 관리자 접속 시 자동 서버 이전
- 사이트/스토리/프로세스: 기존 브라우저 값을 우선 fallback으로 읽고, 저장 버튼을 누르면 D1/R2에 확정 저장
- 서버 데이터를 읽은 뒤 localStorage에는 캐시를 남겨 일시적인 네트워크 오류 시 기존 화면이 유지되도록 구성

## R2 용량 누수 방지
- 사이트/스토리/프로세스 이미지 교체 시 이전 R2 관리 이미지 삭제
- GALLERY 이미지 삭제/교체 시 이전 R2 관리 이미지 삭제
- 프로젝트 이미지 교체 및 프로젝트 영구 삭제 시 기존 R2 관리 이미지 삭제
- 정적 `/images/...` 파일은 R2 관리 파일이 아니므로 삭제 대상에서 제외

## 검수 결과
- Cloudflare Functions 새/수정 JavaScript 파일 `node --check` 문법 검사 통과
- 프로젝트 저장 API: 관리자 세션 확인 → 이미지 R2 저장 → D1 저장 순서 확인
- 갤러리 저장 API: 관리자 세션 확인 → 이미지 R2 저장 → D1 저장 확인
- 사이트/페이지 콘텐츠 API: 관리자 저장과 공개 읽기 API 분리 확인
- 공개 프로젝트는 `published` 상태만 표시하도록 유지
- 새 프로젝트 기본 공개 상태 유지
- 서버 저장 성공 응답 이후 관리자 완료 메시지가 표시되도록 유지/수정
- R2 바인딩이 없을 때 명확한 오류 메시지를 반환하도록 유지
- D1 테이블은 최초 API 호출 시 자동 생성되므로 별도 SQL 실행 불필요

## 빌드 검수 제한
현재 검수 환경의 npm 사설 레지스트리에서 `@types/node` 패키지를 제공하지 않아 `npm install`을 완료할 수 없었습니다. 따라서 Next.js 전체 빌드 검사는 이 환경에서 실행하지 못했습니다. 대신 Cloudflare Functions 문법 검사와 변경 코드의 저장/조회/삭제 경로를 정적 검수했습니다. 배포 시 Cloudflare Pages의 실제 빌드 로그에서 최종 Next.js 빌드 성공 여부를 한 번 확인해 주세요.

## Cloudflare에서 딱 한 번 해야 하는 설정
1. Cloudflare → R2에서 버킷 생성 (예: `fengshui-media`)
2. 현재 Pages 프로젝트 → Settings → Bindings
3. R2 bucket binding 추가
4. Variable name을 정확히 `PROJECT_MEDIA`로 입력
5. 생성한 R2 버킷 선택
6. 기존 D1 binding 이름이 `DB`인지 확인
7. 재배포

이후 관리자 사용법은 기존과 같습니다. 사진 선택 → 내용 입력 → 저장만 하면 됩니다.
